name: test
on: [push]
  
jobs:
  clone:
    runs-on: self-hosted
    steps:
      - name: clone repo and replace 
        run: |
          git clone https://github.com/loictlpk/ghaTest.git $HOME/pfsense
          cp $HOME/pfsense/ansible/inventory/invemtory.yml /etc/ansible/hosts.yml
          