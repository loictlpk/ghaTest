---
- name: pfsenseSudo
  hosts: ssh-server

  tasks:
  - name: install sudo pkg on pfsense
    package:
      name:
        - sudo
      state: latest

  - name: add adservers alias
    ansible.builtin.hostname:
      name: pfloic
      use: systemd

