---
- name: pfsenseSudo
  hosts: ssh-server

  tasks:
  - name: install sudo pkg on pfsense
    package:
      name:
        - sudo
      state: latest

# # change hostname OK
  # - name: test pfsensible
  #   pfsensible.core.pfsense_setup:
  #     hostname: TESTHN
  #     domain: arpa.home

#FIREWALL RULE : block the ping
  - name: test pfsensible
    pfsensible.core.pfsense_rule:
      name: 'Disable ping'
      action: reject
      interface: wan
      ipprotocol: inet
      protocol: icmp
      icmptype: echoreq
      source: any
      destination: any    
