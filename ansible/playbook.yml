---
- name: pfsenseSudo
  hosts: ssh-server

  tasks:
  - name: install sudo pkg on pfsense
    package:
      name:
        - sudo
      state: latest
# change hostname
  # - name: change hostname
  #   ansible.builtin.hostname:
  #     name: pfloic

# add del user
  - name: add adservers alias
    ansible.builtin.user:
      name: loic
      group: sudo
      createhome: yes        # Defaults to yes
      home: /home/loic    # Defaults to /home/<username>